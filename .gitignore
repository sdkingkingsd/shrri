# Local secrets — never commit
shrri_config_local.py
*.pyc
__pycache__/

# Runtime data & logs
*.log
*.db
*.sqlite

# These live in ~/.shrri/, not here — just in case
keys.yaml
gmail_credentials.json
gmail_token.json
contacts_token.json
mcp_servers.json

# Large binary voice models
piper_voices/*.onnx
piper_voices/*.onnx.json
piper_voices/*.wav

# Screenshots and debug output
*.png
*.txt
